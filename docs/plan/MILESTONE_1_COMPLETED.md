# ✅ אבן דרך א': חוויית המשתמש והליבה (הושלמה)

**מבחן אבן הדרך:** אדם זר מסיים סידור שבועי מלא תוך 10 דקות, בלי שאף אחד יסביר לו כלום.

---

### T1 · תיקון מנוע ההחלפות (באג P0 — FR-4.4)
- **הבעיה שנפתרה:** בעבר `decideSwap` שינה סטטוס בלבד ללא בדיקת אילוצי מנוחה וחוק.
- **מה בוצע:**
  - [x] `scripts/verify-scheduler.mjs` — בדיקה אוטומטית: אישור החלפה שמפרה `minRestHours` נדחה.
  - [x] `src/lib/autoAssign.js` — חשיפת `checkHardConstraints` כ-export ציבורי.
  - [x] `src/components/GuardApp.jsx` `MySwaps` — שער בדיקה גם בצד המאבטח.
  - [x] `src/lib/api.js` `decideSwap` — באישור: הרצת `unassignGuard(from)` + `assignGuard(to)` בטרנזקציה אחת.
  - [x] `src/components/supervisor/views.jsx` `SwapMgmt` — הצגת תוצאת בדיקת האילוצים וחסימת כפתור האישור אם יש הפרה.

---

### T2 · מילון מונחים אינטואיטיבי (FR-1.4)
- **מה בוצע:**
  - [x] `SupervisorApp.jsx` `NAV` — שיבוץ חכם ← **סדר לי את השבוע** · פרסום סידור ← **שלח לצוות** · זמינות שומרים ← **מי הגיש** · ניהול משמרות ← **בניית השבוע**.
  - [x] `GuardApp.jsx` `NAV` — הגשת זמינות ← **מתי אני יכול** · הסידור שלי ← **התורנויות שלי**.
  - [x] ריכוז כל המונחים ב-`src/lib/terms.js` כמפה מרכזית אחת כתשתית לפרופילים.

---

### T3 · מסך "השבוע" המאוחד (FR-1.1, FR-1.2, FR-1.7)
- **מה בוצע:**
  - [x] `src/components/supervisor/WeekFlow.jsx` — פס 4 שלבים (ניווט + סטטוס + מונים).
  - [x] עטיפת הרכיבים: `ShiftMgmt` / `AvailView` / `SmartAssign` / `ScheduleMgmt`.
  - [x] `AssignView` (שיבוץ ידני) ממוזג לתוך שלב 3 (עריכה בתוך התוצאה).
  - [x] תפריט הניווט צומצם ל-4 פריטים: השבוע · יומן · הצוות · עוד.

---

### T4 · פעולה ראשית ומצבים ריקים (FR-1.3, FR-1.6)
- **מה בוצע:**
  - [x] `ui.jsx` — רכיב `PrimaryAction` בתחתית המסך ברוחב מלא.
  - [x] `ui.jsx` `EmptyState` — תמיכה בצעד הבא ותבניות מוכנות (3 משמרות ביום, בוקר+לילה, בנייה ידנית).

---

### T5 · ביטול במקום אישור (FR-1.5)
- **מה בוצע:**
  - [x] `ui.jsx` — רכיב `UndoBar` עם רצועת ביטול ל-8 שניות.
  - [x] `useGuardian.js` — פונקציית `deferred` לעדכון מסך מיידי והשהיית כתיבה.
  - [x] אפס חלוניות אישור מציקות (`confirm()`).

---

### T6 · מסך פותח למשתתף ושורת הוגנות (FR-2.1, FR-2.2)
- **מה בוצע:**
  - [x] `GuardApp.jsx` — כרטיס פותח `NextDuty` עם ספירה לאחור במילים.
  - [x] `autoAssign.js` — פונקציית `teamAverages` לחישוב ממוצעי צוות.
  - [x] `FairnessLine` — מונים צבעוניים מול ממוצע הצוות.

---

### T7 · עבודה באופליין ללא רשת (FR-2.3)
- **מה בוצע:**
  - [x] `public/manifest.json` + `public/icon.svg` + `public/sw.js`.
  - [x] `useGuardian.js` — צילום נתונים ב-`localStorage` לטעינה כשאין חיבור.
  - [x] רצועת "אין חיבור" מעוצבת ורענון אוטומטי בחזרת הרשת.

---

### T8 · שיתוף סידור כתמונה (FR-4.1)
- **מה בוצע:**
  - [x] `src/lib/shareImage.js` — רינדור הסידור ל-Canvas וייצוא PNG ברוחב 1080px.
  - [x] תמיכה ב-`navigator.canShare` עם נפילה נקייה להורדה ישירה.
  - [x] פונקציית `drawLtr` לתיקון כיווניות שעות המשמרת בעברית.
