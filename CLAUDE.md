# 🤖 CLAUDE.md — NexRota

הקובץ הזה נטען בכל שיחה, ולכן הוא **אינדקס בלבד**. כל פירוט יורד ל-`docs/` ונקרא רק כשצריך אותו.

---

## ⚡ פקודות

| | |
| :--- | :--- |
| בדיקות | `npm test` — מנוע השיבוץ + התנגשויות והוגנות |
| בדיקה בודדת | `node scripts/verify-scheduler.mjs` / `node scripts/verify-planning.mjs` |
| בדיקת Backend | `npm run test:backend` — **לא** חלק מ-`npm test`: פוגע ב-Supabase האמיתי ויוצר חשבונות זמניים |
| בנייה | `npm run build` |
| שרת פיתוח | `npm run dev` (פורט 3000 — כתובות החזרה של Supabase מכוונות לשם) |
| שרת עם פורט אוטומטי | `npm run dev:auto` |

אין בפרויקט runner בדיקות ואין linter. הבדיקות הן סקריפטי Node עצמאיים שמדפיסים `ok`/`FAIL` ומחזירים קוד יציאה.

---

## 🛡️ עקרונות ברזל

1. **דטרמיניזם מוחלט** — אין `Math.random()` במנוע השיבוץ. אותם נתונים ⟵ אותו סידור, תמיד.
2. **אילוץ קשיח קודם לכל** — שיבוץ או החלפה שמפרים חוק עבודה (8 שעות מנוחה, 12 שעות רצופות, איסור חפיפה, תקרת 6 משמרות) נדחים.
3. **ביטול במקום אישור** — הפעולה מתבצעת מיד, עם `UndoBar` של 8 שניות. בלי `confirm()`.
4. **עבודה ללא רשת** — הסידור נשמר ב-`localStorage` ונקרא גם בלי חיבור.
5. **`npm test` ו-`npm run build`** עוברים אחרי כל שינוי.
6. **אימות בדפדפן** — "עובד" נאמר רק אחרי שראית את זה עובד. מה שלא נבדק מדווח כלא נבדק.

---

## 🧭 ארבע נקודות חנק בקוד

לפני שינוי, כדאי לדעת מי הבעלים של מה. הפירוט ב-[ארכיטקטורה](docs/architecture/system-overview.md).

| הקובץ | מה עובר דרכו — ולמה לא לעקוף אותו |
| :--- | :--- |
| `src/hooks/useGuardian.js` | ה-State היחיד: session, פרופיל, וכל נתוני הצוות. כל עדכון אופטימי חייב rollback, וכל פעולה מדווחת כישלון. `actions` יציב רפרנציאלית — אסור לבנות אותו מחדש. |
| `src/lib/api.js` | המקום **היחיד** שמכיר שמות עמודות ב-DB. מעליו הכל camelCase, מתחתיו snake_case. רכיב שקורא `start_time` הוא באג. |
| `src/lib/autoAssign.js` | המנוע: אילוצים קשיחים מסננים ⟵ קריטריונים רכים מנקדים ⟵ מילוי most-constrained-first ⟵ מעבר איזון. כל החלטה נושאת נימוק קריא. `shiftLoad()` הוא יחידת ההוגנות היחידה במוצר — גם המסך של המשתתף חייב להציג אותה. |
| `src/lib/terms.js` | כל מילה שהמשתמש רואה. מונחים משתנים לפי `gs_teams.mode` (`civil` / `army`), ולכן אין מחרוזת ממשק קשיחה ברכיבים. תחום הפעילות מוחל מ-`useGuardian` ולא מהמסך שמשנה אותו. |
| `src/lib/conflicts.js` + `src/lib/fairness.js` | שני מנועים טהורים נוספים: מה אסור לחפוף, ומי צריך לקבל עוד. שניהם נבדקים ב-`verify-planning.mjs`. |

הממשק כולו עברית ו-RTL. Supabase הוא גם auth וגם RLS — מדיניות RLS היא חלק מההתנהגות, לא תשתית נסתרת.

---

## 📚 מפת התיעוד

| מסמך | השאלה שהוא עונה עליה |
| :--- | :--- |
| [חבילת הקשר](docs/product/CONTEXT_PACK.md) | **מה קיים היום בפועל** — וגם מה להעלות למודל חיצוני |
| [PRD](docs/product/PRD.md) | למה המוצר קיים, למי, ומה נדרש ממנו (חזון) |
| [מילון המוצר](docs/product/glossary.md) | איך קוראים לדברים בממשק, ולמה |
| [תפקידים וסוכנים](docs/agents/ROLES.md) | מי אחראי על מה, ואילו skills לשלוף |
| [תוכנית האב](docs/plan/MASTER_PLAN.md) | איפה הפרויקט עומד |
| [אבן דרך א'](docs/plan/MILESTONE_1_COMPLETED.md) | מה כבר נבנה |
| [אבן דרך ב'](docs/plan/MILESTONE_2_ROADMAP.md) | מה הבא בתור |
| [ארכיטקטורה](docs/architecture/system-overview.md) | איפה נמצא כל דבר בקוד |
| [מנוע השיבוץ](docs/algorithm/auto-assign.md) | איך המערכת מחליטה מי משובץ |
| [בסיס נתונים](docs/database/schema-and-rls.md) | טבלאות Supabase ומדיניות RLS |

---

## 👥 תפקידים

כתיבת כינוי תפקיד בהודעה (`FRONTEND`, `UX`, `ALGO`, `QA`, `DEVOPS`, `PMO`, `BRAND`) מעבירה אותי לכובע המתאים.
**אלה פרסונות, לא Agents** — הפירוט וההסבר ב-[ROLES.md](docs/agents/ROLES.md).

---

## ✍️ כשמוסיפים תיעוד

- **קובץ אחד = שאלה אחת.** מפצלים לפי נושא, לא לפי אורך.
- **`.agents/skills/` לא נערך ידנית** — 253 קבצים מנוהלים דרך `skills-lock.json`.
- **CLAUDE.md לא גדל.** תוכן חדש הולך ל-`docs/`, וכאן מופיעה רק שורה בטבלה.
